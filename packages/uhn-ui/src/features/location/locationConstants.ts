/** Height of the UXP shell's fixed AppBar in pixels. */
export const APP_BAR_HEIGHT = 64;

/** Combined offset for content below both AppBar and sticky switcher bar. */
export const STICKY_OFFSET = 120;

/** Duration (ms) to suppress intersection observer during programmatic smooth scroll. */
export const SCROLL_OVERRIDE_TIMEOUT = 800;
